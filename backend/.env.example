# Carbonly Gateway Environment Configuration Example

# Gateway Server Configuration
PORT=5000
NODE_ENV=production

# Groq AI Decision Intelligence Service Key (Optional - Deterministic fallback activated if empty)
GROQ_API_KEY=your_groq_api_key_here
